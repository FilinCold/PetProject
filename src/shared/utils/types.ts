export enum PageNames {
  PROFILE = 'profile',
  ABOUT = 'about',
  NEWS = 'news',
  BLOGS = 'blogs',
  ARTICLES = 'articles',
}

export interface LayoutSettings {
  isHeader?: boolean;
  isFooter?: boolean;
  isSideBar?: boolean;
}
